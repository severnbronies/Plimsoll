{% from "@components/footer/macro.twig" import sbFooter %}

{{ sbFooter({
	newsletter: {
		show: newsletter_show,
		heading: newsletter_heading,
		description: newsletter_description,
		placeholder: newsletter_placeholder
	},
	navItems: footer_navigation.get_items,
	legalText: footer_legal_copy
}) }}