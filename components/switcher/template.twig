<div
    data-module="sb-switcher"
    class="sb-switcher {%- if params.classes %} {{ params.classes }}{% endif %}"
    {%- for attribute, value in params.attributes %} {{ attribute }}="{{ value }}"{% endfor %}>
    {{ params.html }}
</div>
