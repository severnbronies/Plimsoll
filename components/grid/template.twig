<div
    data-module="sb-grid"
    class="sb-grid {%- for v in params.variants %} sb-grid--{{ v }}{% endfor %} {%- if params.classes %} {{ params.classes }}{% endif %}"
    {%- for attribute, value in params.attributes %} {{ attribute }}="{{ value }}"{% endfor %}>
    {{ params.html }}
</div>
